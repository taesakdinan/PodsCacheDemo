# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PodsCacheDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PodsCacheDemo
  # networking
  pod 'Alamofire'
  # JSON mapper
  pod 'ObjectMapper', '~> 3.5'
  # Reactive
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
  # Images
  pod 'Kingfisher', '~> 5.0'
end
